# GUI (Streamlit) 전용 의존성
streamlit>=1.32
pandas>=2.0
# Streamlit이 내부적으로 사용
watchdog>=3.0; sys_platform != "win32"

# 본 프로젝트 본체 (이미 설치돼 있다면 무시)
python-dateutil>=2.8.2

# 구글 시트(선택)
gspread>=6.0.0
google-auth>=2.27.0

# 환경변수 자동 로드(선택)
python-dotenv>=1.0.0
